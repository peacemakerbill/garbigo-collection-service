package com.garbigo.collection.model;

/** Driven by garbigo-wallet-service (not yet built), not by this service. */
public enum PaymentStatus {
    UNPAID,
    PENDING,
    PAID,
    FAILED,
    REFUNDED
}