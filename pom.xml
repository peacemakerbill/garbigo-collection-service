<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.garbigo</groupId>
    <artifactId>garbigo-collection-service</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>garbigo-collection-service</name>
    <description>
        Garbigo microservice owning pickup requests, scheduling, collector
        assignment, and complaints. Identity lives in garbigo-auth-service;
        payments are deferred to garbigo-wallet-service. See README.md for
        scope and the auth-service integration contract.
    </description>

    <properties>
        <java.version>21</java.version>
        <!--
          Spring Cloud release train compatible with Spring Boot 4.1.x
          (needed for spring-cloud-starter-openfeign below). Boot and Cloud
          versions are paired and move together - re-check
          https://github.com/spring-cloud/spring-cloud-release/wiki before
          bumping either independently.
        -->
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <!--
          jjwt is this scaffold's best guess at auth-service's JWT library,
          not a confirmed fact - swap JwtUtil (and this dependency) for
          whatever auth-service actually uses once its real JwtUtil is
          shared.
        -->
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!--
          Web layer. Spring Boot 4's modular starters split this out of the
          old "spring-boot-starter-web" - the servlet-stack starter is now
          "-webmvc".
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <!-- Spring Security 7.1.1 (pulled in via the Boot parent). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- This service's own MongoDB database: garbigo_collection -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>

        <!--
          Redis. Must point at the SAME instance auth-service uses - this is
          purely for reading auth-service's revoked:jti:* denylist keys, not
          for this service's own caching.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Same RabbitMQ broker as auth-service; consumes user-created-queue -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <!-- Jakarta Bean Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- OpenFeign - new to this service. AuthServiceClient/WalletServiceClient call out to other services. -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>

        <!--
          JWT validation (see the jjwt.version note above). jjwt-impl and
          jjwt-jackson are runtime-only implementation details behind the
          jjwt-api interfaces JwtUtil actually codes against.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!--
          Lombok, to cut boilerplate on the model/DTO classes. Drop this (and
          the matching annotation-processor exclusion below) if auth-service
          doesn't actually use it - swap in plain getters/setters instead.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
